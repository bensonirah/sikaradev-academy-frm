function printMath() {
  // Math.round(x)
  //   Math.ceil(x);
  // Math.floor(x)
  //   Math.trunc(x);
  //   Math.pow(x, y);
  // Math.sqrt(x)
  //   Math.abs();
  // Math.min(x)
  //   Math.max(x);
  //   Math.random();
  // 4<4.1<4.2<5
  // 0<x<1
  for (let step = 0; step < 5; step++) {
    console.log(Math.trunc(Math.random() * 10));
  }
}

module.exports = printMath;

// https://web.facebook.com/watch/?ref=search&v=381020364159529&external_log_id=324c984d-e429-4f5e-9903-ec9650c42623&q=prison%20break
