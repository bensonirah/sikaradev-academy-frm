function printString() {
  const message =
    'Hi every one, we are going to master "javascript" \nthrought this formation!';
  // <string>[index]
  // <string>.property
  // <string [\escap_me]>
  // <string>.method()
  console.log(message.slice(0, 8));
}

module.exports = printString;
